// Needed for the developer environment inside Danske Bank.
if (artifactory_joinedUrl != '') {
    pluginManagement {
        repositories {
            maven {
                name = 'joined-gradle'
                url artifactory_joinedUrl
                credentials {
                    username = artifactory_user
                    password = artifactory_password
                }
            }
            gradlePluginPortal()
        }
    }
}

rootProject.name = "event-router"